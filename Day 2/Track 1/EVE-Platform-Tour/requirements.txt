# Agentic_EVE.ipynb — direct dependencies (see also Setup cell in the notebook).
python-dotenv>=1.0.0
httpx>=0.27.0
geopy>=2.4.0
requests>=2.31.0
fastmcp>=3.0.0

# Used for trace HTML rendering in the notebook (IPython.display).
ipython>=8.12.0
